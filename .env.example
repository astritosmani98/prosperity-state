# Copy to ".env" and fill in to enable game records (optional).
# Get this from your Neon project dashboard → Connection string (includes ?sslmode=require).
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx-xxxx.REGION.aws.neon.tech/DBNAME?sslmode=require
